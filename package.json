{
  "name": "robtrade-robinhood-app",
  "version": "0.2.0",
  "private": true,
  "description": "RobTrade Robinhood Chain project — one-command local frontend + backend development.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "backend": "npm --prefix backend run dev",
    "zk:compile": "node scripts/zk-compile.mjs",
    "zk:check": "node scripts/zk-check.mjs"
  },
  "dependencies": {
    "snarkjs": "^0.7.6"
  },
  "engines": {
    "node": ">=20"
  }
}
